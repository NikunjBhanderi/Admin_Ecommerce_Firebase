class UpdateModel {
  String? name, surname, job, mobile, email, types, key, fcmKey;

  UpdateModel({
    this.name,
    this.surname,
    this.job,
    this.mobile,
    this.email,
    this.types,
    this.key,
    this.fcmKey,
  });
}
