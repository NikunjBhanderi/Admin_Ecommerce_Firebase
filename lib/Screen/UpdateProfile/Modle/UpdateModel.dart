class UpdateModel {
  String? name, surname, job, mobile, email, types,key;

  UpdateModel({
    this.name,
    this.surname,
    this.job,
    this.mobile,
    this.email,
    this.types,
    this.key
  });
}
