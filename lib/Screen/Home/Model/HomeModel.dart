class HomeModel {
  String? brand, desc, image, name, key;
  int? price, stoke, status;
  double? rate;
  bool? isButton, isLike;

  HomeModel({
    this.brand,
    this.isButton,
    this.desc,
    this.image,
    this.name,
    this.price,
    this.rate,
    this.stoke,
    this.key,
    this.isLike,
    this.status,
  });
}
