class HomeModel {
  String? brand, desc, image, name, key;
  int? price, stoke;
  double? rate;
  bool? isButton;

  HomeModel({
    this.brand,
    this.isButton,
    this.desc,
    this.image,
    this.name,
    this.price,
    this.rate,
    this.stoke,
    this.key,
  });
}
